# Rust
target/
**/target/     # For nested target directories
**/*.rs.bk

# Node.js
node_modules/
/dist/
/.svelte-kit/
/package/
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# SQLite database files only (not the migrations or .sqlx)
server/database/*.db
server/database/*.db-shm
server/database/*.db-wal
*.bak

# SQLx offline query cache
# Keep `server/.sqlx/` tracked when building with `SQLX_OFFLINE=true` (see `server/Dockerfile`).

# Environment files
*.env
.env
.env.*
!.env.example
**/server.env
**/client.env
client/client.env

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
logs/
.dev/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build
/build
client/build/
.svelte-kit
/package 

# (Former Playwright) leftover artifacts
client/test-results/
client/playwright-report/

# Playwright MCP output + ad-hoc verification screenshots at repo/client root
**/.playwright-mcp/
/*.png
/client/*.png


# backups
server/backups/

# mockup-only screenshot captures (local visual audits)
mockups/.shots/
.superpowers/

# CodeGraph local index (machine-local, do not commit)
.codegraph/
