[
  {
    "idx": 1,
    "verdicts": [
      {
        "title": "Unbounded N+1 query fan-out in progress/exercise reads",
        "refuted": false,
        "reason": "Code confirms the claim exactly: get_exercise_progress (progress.rs:359-374) issues an unbounded `SELECT ... FROM exercises WHERE user_id = ?` with no LIMIT, then at lines 377-399 loops over every row calling self.get_sets_for_exercise(...).await? and self.get_settings_for_exercise(...).await? per exercise — two sequential round-trip queries per row, count proportional to attacker/agent-controlled row volume. No pagination, LIMIT, batching, or JOIN collapses this into fewer queries. The mechanism is real as described."
      },
      {
        "title": "Progress overview loads entire user set history into memory and recomputes PRs in Rust on every call",
        "refuted": false,
        "reason": "Confirmed. get_progress_set_facts() (progress.rs:968-1022) runs a single SELECT joining sets/exercises/workouts scoped by user_id with no LIMIT and no time-window predicate, fetch_all-ing every set the user has ever logged into a Vec<SetFact>. get_progress_overview() (line 771-773) calls this on every invocation and then runs detect_pr_events()/detect_recent_best_events() over the full in-memory Vec before doing further period-scoped queries. There is no caching between calls and no bound on set count, so the claimed O(total sets) memory/CPU cost per request is accurate as written."
      }
    ]
  }
]
